applicationType: default
defaultPackage: com.andrecastrosousa
testFramework: junit
sourceLanguage: kotlin
buildTool: gradle
features: [app-name, graalvm, gradle, http-client-test, junit, kotlin, kotlin-application, ksp, logback, micronaut-aot, micronaut-build, micronaut-http-validation, mongo-reactive, netty-server, reactor, readme, serialization-bson, serialization-jackson, shade, test-resources, yaml, yaml-build]
